{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/areafadaos run build",
  "outputDirectory": "web/dist/public",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
